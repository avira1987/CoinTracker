# راهنمای مشارکت در CoinTracker

از مشارکت شما در این پروژه خوشحالیم! این سند راهنمای نحوه مشارکت است.

## 🚀 شروع کار

1. **Fork کنید**: روی دکمه Fork در بالای صفحه کلیک کنید
2. **Clone کنید**: 
   ```bash
   git clone https://github.com/yourusername/CoinTracker.git
   cd CoinTracker
   ```
3. **Branch جدید بسازید**: 
   ```bash
   git checkout -b feature/your-feature-name
   ```

## 📝 استانداردهای کد

### Python (Backend)

- از **PEP 8** پیروی کنید
- **Docstrings** برای توابع و کلاس‌ها (به فارسی یا انگلیسی)
- **Type hints** در صورت امکان
- نام‌گذاری معنادار و واضح

مثال:
```python
def calculate_rank_score(self, cryptocurrency, all_cryptocurrencies):
    """
    محاسبه نمره رتبه‌بندی با فرمول وزنی
    
    Args:
        cryptocurrency: ارز دیجیتال مورد نظر
        all_cryptocurrencies: لیست تمام ارزهای دیجیتال
    
    Returns:
        Decimal: نمره رتبه‌بندی
    """
    # کد...
```

### JavaScript (Frontend)

- از **ESLint** استفاده کنید
- کامنت‌های واضح برای منطق پیچیده
- نام‌گذاری معنادار
- استفاده از const/let به جای var

مثال:
```javascript
// محاسبه تغییرات قیمت
const calculatePriceChange = (currentPrice, previousPrice) => {
  if (!previousPrice || previousPrice === 0) return 0;
  return ((currentPrice - previousPrice) / previousPrice) * 100;
};
```

## 🔄 فرآیند Pull Request

1. **تغییرات خود را commit کنید**:
   ```bash
   git add .
   git commit -m "Add: توضیح واضح از تغییرات"
   ```

2. **Push کنید به branch خود**:
   ```bash
   git push origin feature/your-feature-name
   ```

3. **Pull Request باز کنید**:
   - به repository اصلی بروید
   - روی "New Pull Request" کلیک کنید
   - branch خود را انتخاب کنید
   - توضیح واضح از تغییرات ارائه دهید

4. **منتظر Review بمانید**:
   - ممکن است درخواست تغییراتی شود
   - لطفاً به نظرات پاسخ دهید

## 📋 قوانین Commit Messages

از فرمت زیر استفاده کنید:

```
Add: اضافه کردن ویژگی جدید
Fix: رفع باگ
Update: به‌روزرسانی کد موجود
Remove: حذف کد یا ویژگی
Docs: تغییرات در مستندات
Style: تغییرات فرمت (فقط فاصله‌گذاری)
Refactor: بازنویسی کد بدون تغییر عملکرد
Test: اضافه یا تغییر تست‌ها
```

مثال:
```
Add: اضافه کردن فیلتر جستجو در Dashboard
Fix: رفع مشکل اتصال WebSocket در Firefox
Update: بهبود الگوریتم محاسبه پایداری
```

## 🐛 گزارش باگ

لطفاً در [Issues](https://github.com/avira1987/CoinTracker/issues) گزارش دهید با:

- **عنوان واضح**: توضیح کوتاه از مشکل
- **توضیح مشکل**: شرح کامل مشکل
- **مراحل بازتولید**: 
  1. رفتن به '...'
  2. کلیک روی '....'
  3. مشاهده خطا
- **رفتار مورد انتظار**: چه اتفاقی باید می‌افتاد
- **رفتار واقعی**: چه اتفاقی افتاده
- **Screenshot**: در صورت امکان
- **محیط**:
  - OS: [e.g. Windows 10, Ubuntu 22.04]
  - Browser: [e.g. Chrome 120]
  - Python Version: [e.g. 3.11]
  - Node Version: [e.g. 18.17]

## ✨ پیشنهاد ویژگی جدید

ایده‌های خود را در [Issues](https://github.com/avira1987/CoinTracker/issues) به اشتراک بگذارید:

- **عنوان**: توضیح کوتاه از ویژگی
- **توضیح**: شرح کامل ویژگی پیشنهادی
- **دلیل**: چرا این ویژگی مفید است
- **جایگزین‌ها**: راه‌حل‌های جایگزین (در صورت وجود)

## 🧪 تست کردن تغییرات

قبل از ارسال Pull Request:

1. **Backend**:
   ```bash
   cd backend
   python manage.py test
   ```

2. **Frontend**:
   ```bash
   cd frontend
   npm run build  # بررسی خطاهای build
   ```

3. **Docker** (در صورت تغییر Dockerfiles):
   ```bash
   docker-compose build
   docker-compose up -d
   ```

## 📚 منابع مفید

- [Django Documentation](https://docs.djangoproject.com/)
- [React Documentation](https://react.dev/)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [Django Channels](https://channels.readthedocs.io/)

## ❓ سوالات

اگر سوالی دارید:

1. ابتدا [Issues](https://github.com/avira1987/CoinTracker/issues) را بررسی کنید
2. اگر پاسخ را پیدا نکردید، Issue جدید باز کنید
3. یا مستقیماً با maintainer تماس بگیرید

## 🙏 تشکر

از مشارکت شما در بهبود این پروژه متشکریم!

---

**یادآوری**: لطفاً قبل از شروع کار روی یک ویژگی بزرگ، ابتدا Issue باز کنید تا مطمئن شویم که کار تکراری انجام نمی‌دهید.
