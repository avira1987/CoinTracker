# 🚀 راهنمای استقرار سریع CoinTracker

این راهنما برای راه‌اندازی سریع پروژه روی سیستم‌های دیگر با Docker طراحی شده است.

## 📋 پیش‌نیازها

- ✅ Docker نصب باشد
- ✅ Docker Compose نصب باشد (یا Docker با plugin compose)
- ✅ حداقل 2GB RAM آزاد
- ✅ حداقل 5GB فضای خالی

## 🎯 روش اجرای سریع (تک فایل)

### برای سیستم‌های Linux/Mac:

```bash
# 1. اجرای مجوز برای اسکریپت
chmod +x deploy.sh

# 2. اجرای اسکریپت
./deploy.sh
```

### برای سیستم‌های Windows:

```cmd
# فقط فایل deploy.bat را دوبار کلیک کنید
# یا در PowerShell:
.\deploy.bat
```

## 📝 مراحل اجرا

اسکریپت به صورت خودکار:

1. ✅ نصب Docker را بررسی می‌کند
2. ✅ وجود `settings.json` را بررسی می‌کند
3. ✅ در صورت نیاز، `settings.json` را از `settings.example.json` ایجاد می‌کند
4. ✅ کانتینرهای قبلی را متوقف می‌کند
5. ✅ کانتینرها را build و اجرا می‌کند
6. ✅ وضعیت سرویس‌ها را نمایش می‌دهد

## ⚙️ تنظیم API Key

پس از اولین اجرا، باید API Key خود را در `settings.json` وارد کنید:

1. فایل `settings.json` را باز کنید
2. مقدار `YOUR_API_KEY_HERE` را با API Key واقعی خود جایگزین کنید
3. می‌توانید API Key رایگان از [CoinGecko](https://www.coingecko.com/api) دریافت کنید

## 🌐 دسترسی به برنامه

پس از اجرای موفقیت‌آمیز:

- **Frontend**: http://localhost
- **Backend API**: http://localhost/api/
- **Admin Panel**: http://localhost/admin/

## 🔑 اطلاعات ورود پیش‌فرض

- **Username**: `admin34_`
- **Password**: `123asd;p+_`

⚠️ **توجه**: حتماً رمز عبور را در production تغییر دهید!

## 🛠️ دستورات مفید

### مشاهده لاگ‌ها:
```bash
docker-compose logs -f
# یا
docker compose logs -f
```

### توقف سرویس‌ها:
```bash
docker-compose down
# یا
docker compose down
```

### راه‌اندازی مجدد:
```bash
docker-compose restart
# یا
docker compose restart
```

### مشاهده وضعیت:
```bash
docker-compose ps
# یا
docker compose ps
```

### اجرای دستورات در Backend:
```bash
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py createsuperuser
```

## 🔄 راه‌اندازی مجدد از صفر

در صورت نیاز به شروع مجدد کامل:

```bash
# توقف و حذف همه کانتینرها و حجم‌ها
docker-compose down -v

# Build مجدد و اجرا
docker-compose up -d --build
```

## 🐛 رفع مشکلات

### مشکل: "Docker is not installed"
- Docker را از [docker.com](https://www.docker.com/get-started) نصب کنید
- Docker Desktop را راه‌اندازی کنید

### مشکل: "Port already in use"
- پورت 80 یا 8000 توسط برنامه دیگری استفاده می‌شود
- در `docker-compose.yml` پورت‌ها را تغییر دهید

### مشکل: "Cannot connect to Docker daemon"
- Docker Desktop را اجرا کنید
- Docker service را راه‌اندازی کنید (Linux: `sudo systemctl start docker`)

### مشکل: "settings.json not found"
- اسکریپت به صورت خودکار این فایل را ایجاد می‌کند
- در صورت نیاز، می‌توانید از `settings.example.json` کپی کنید

## 📦 استقرار روی سرور

برای استقرار روی سرور با IP مشخص:

1. **تنظیم ALLOWED_HOSTS** در `backend/config/settings.py`:
   ```python
   ALLOWED_HOSTS = ['your-server-ip', 'your-domain.com']
   ```

2. **اجرای اسکریپت deploy**:
   ```bash
   ./deploy.sh  # Linux/Mac
   # یا
   deploy.bat   # Windows
   ```

3. **تنظیم فایروال** (در صورت نیاز):
   - پورت 80 باید باز باشد
   - پورت 8000 برای دسترسی مستقیم به Backend (اختیاری)

## 🔒 نکات امنیتی

- ✅ رمز عبور پیش‌فرض را تغییر دهید
- ✅ برای production، `DEBUG = False` تنظیم کنید
- ✅ از HTTPS استفاده کنید (با Nginx + Let's Encrypt)
- ✅ API Key را در `.gitignore` قرار دهید

## 📞 پشتیبانی

در صورت بروز مشکل، لاگ‌ها را بررسی کنید:
```bash
docker-compose logs backend
docker-compose logs frontend
docker-compose logs nginx
```

---

**نکته**: این اسکریپت برای سهولت استفاده طراحی شده است. برای استقرار production، تنظیمات امنیتی و بهینه‌سازی بیشتری لازم است.
