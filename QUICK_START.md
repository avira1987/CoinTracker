# ⚡ راه‌اندازی سریع (یک دستور)

برای راه‌اندازی پروژه روی سیستم جدید با Docker:

## ⚠️ نکته مهم (Windows):
اگر پوشه پروژه `CoinTracker-main` نام دارد، ابتدا نام آن را تغییر دهید:
```cmd
ren CoinTracker-main CoinTracker
cd CoinTracker
```

## Linux/Mac:
```bash
chmod +x deploy.sh && ./deploy.sh
```

## Windows:
```cmd
deploy.bat
```

یا اگر خطا گرفتید، از نسخه ایمن استفاده کنید:
```cmd
deploy-safe.bat
```

یا اگر PowerShell دارید:
```powershell
.\deploy.bat
```

**مشکل دارید؟** [DEPLOY_TROUBLESHOOTING.md](DEPLOY_TROUBLESHOOTING.md) را مطالعه کنید.

---

## 📝 بعد از اجرا:

1. API Key را در `settings.json` وارد کنید
2. منتظر بمانید تا کانتینرها build شوند (اولین بار چند دقیقه طول می‌کشد)
3. به http://localhost مراجعه کنید

---

**ورود پیش‌فرض:**
- Username: `admin34_`
- Password: `123asd;p+_`

---

📖 برای اطلاعات بیشتر، [DEPLOY.md](DEPLOY.md) را مطالعه کنید.
